if(process.env.NODE_ENV!=='production'){const fs=require('fs');const hook='#!/bin/sh\n# Post-commit hook - Updates dependency graph after each commit\necho "🔍 Updating dependency graph..."\nunset LD_LIBRARY_PATH && npx tsx scripts/analyze-imports.ts';try{fs.writeFileSync('.git/hooks/post-commit',hook);fs.chmodSync('.git/hooks/post-commit','755');console.log('✓ post-commit hook installed')}catch(e){}}